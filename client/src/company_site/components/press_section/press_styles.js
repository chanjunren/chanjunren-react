import { makeStyles } from '@material-ui/core/styles';

const PressStyles = makeStyles((theme) => ({

  pressContainer: {
    padding: '20px',
    height: 'auto',
    // background: '#1B2026',
  },
  header: {
    padding: '30px',
  },
}));

export default PressStyles;
