import React from 'react';
import { Button } from '../shared/button';

import {InfoContainer, InfoWrapper} from './info_elements';

const ProductsSection = () => {
  return (
    <InfoContainer id='products' lightBg={true}>
      <InfoWrapper>
        
      </InfoWrapper>
    </InfoContainer>
  );
};

export default ProductsSection;
